{
  "name": "playground-sqlite",
  "description": "Testing sqlite",
  "repository": "yoshuawuyts/playground-sqlite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov"
  },
  "dependencies": {
    "sqlite3": "^3.1.8"
  },
  "devDependencies": {
    "dependency-check": "^2.8.0",
    "nyc": "^10.2.0",
    "standard": "^10.0.2",
    "tape": "^4.6.3"
  }
}
